---
phase: "03.1"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Nomad/Nomad.entitlements
  - Nomad/Data/CityCluster.swift
  - Nomad/Data/WeatherService.swift
  - Nomad/Info.plist
autonomous: true
requirements: ["SC-3", "SC-5"]

must_haves:
  truths:
    - "WeatherKit entitlement is present in Nomad.entitlements"
    - "CityCluster groups trips by geographic proximity with configurable radius"
    - "WeatherService fetches current temperature and returns formatted string or nil"
  artifacts:
    - path: "Nomad/Nomad.entitlements"
      provides: "WeatherKit entitlement"
      contains: "com.apple.developer.weatherkit"
    - path: "Nomad/Data/CityCluster.swift"
      provides: "CityCluster model + clusterTripsByProximity function"
      exports: ["CityCluster", "clusterTripsByProximity"]
    - path: "Nomad/Data/WeatherService.swift"
      provides: "Temperature fetch wrapper"
      exports: ["NomadWeatherService"]
  key_links:
    - from: "Nomad/Data/CityCluster.swift"
      to: "TripDocument"
      via: "import and grouping"
      pattern: "TripDocument"
---

<objective>
Set up the WeatherKit entitlement (Wave 0 blocker) and create the foundational data models that all subsequent plans depend on: CityCluster (proximity-based trip grouping) and NomadWeatherService (temperature fetch wrapper).

Purpose: WeatherKit crashes at runtime without the entitlement — this must be added before any WeatherKit code. CityCluster and WeatherService are consumed by Plans 02 and 03.
Output: Entitlement file updated, two new Swift files committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-country-detail-view/03.1-CONTEXT.md
@.planning/phases/03.1-country-detail-view/03.1-UI-SPEC.md
@.planning/phases/03.1-country-detail-view/03.1-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from the existing codebase. -->

From Nomad/Data/Models/TripDocument.swift:
```swift
struct TripDocument: Identifiable {
    let id: String
    let cityName: String
    let startDate: Date
    let endDate: Date
    let stepCount: Int
    let distanceMeters: Double
    let routePreview: [[Double]]       // [[lat, lon], ...]
    let visitedCountryCodes: [String]
    let placeCounts: [String: Int]

    var coordinate: CLLocationCoordinate2D? {
        guard let first = routePreview.first, first.count >= 2 else { return nil }
        return CLLocationCoordinate2D(latitude: first[0], longitude: first[1])
    }
}
```

From Nomad/Nomad.entitlements (current state — only HealthKit):
```xml
<dict>
    <key>com.apple.developer.healthkit</key>
    <true/>
</dict>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WeatherKit entitlement and create CityCluster data model</name>
  <files>Nomad/Nomad.entitlements, Nomad/Data/CityCluster.swift</files>
  <action>
1. Update `Nomad/Nomad.entitlements` — add `com.apple.developer.weatherkit` key with boolean `true` value, alongside the existing `com.apple.developer.healthkit` entry.

2. Create `Nomad/Data/CityCluster.swift` with:
   - `struct CityCluster: Identifiable` with fields: `let id = UUID()`, `var cityName: String` (most common cityName among member trips), `var centroid: CLLocationCoordinate2D`, `var trips: [TripDocument]`.
   - Computed properties: `var tripCount: Int { trips.count }`, `var totalDistanceKm: Double { trips.reduce(0) { $0 + $1.distanceMeters } / 1000 }`, `var dateRange: (start: Date, end: Date)` (min startDate, max endDate across trips).
   - Free function `func clusterTripsByProximity(_ trips: [TripDocument], radiusKm: Double = 50.0) -> [CityCluster]`:
     - Greedy centroid clustering: iterate trips, for each trip with a non-nil `coordinate`, find existing cluster within `radiusKm` using `CLLocation.distance(from:)` / 1000. If found, append trip; otherwise create new cluster.
     - After clustering, set each cluster's `cityName` to the most frequently occurring `cityName` among its member trips.
     - Sort clusters by trip count descending (most-visited city first).
   - The 50.0 km default is a constant `let kCityClusterRadiusKm: Double = 50.0` at file scope.

Import CoreLocation and reference TripDocument (no import needed — same module).
  </action>
  <verify>
    <automated>cd /Users/shrey/nomad-final/nomadv2 && xcodebuild -scheme Nomad -destination 'platform=iOS Simulator,name=iPhone 16 Pro' build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Nomad.entitlements contains both `com.apple.developer.healthkit` and `com.apple.developer.weatherkit`
    - CityCluster.swift compiles with CityCluster struct and clusterTripsByProximity function
    - kCityClusterRadiusKm constant is exposed at file scope for configurability
  </done>
</task>

<task type="auto">
  <name>Task 2: Create NomadWeatherService wrapper</name>
  <files>Nomad/Data/WeatherService.swift</files>
  <action>
Create `Nomad/Data/WeatherService.swift` with:

1. `import WeatherKit` and `import CoreLocation`.
2. `@MainActor final class NomadWeatherService` (no @Observable needed — this is a stateless service).
3. Static shared instance: `static let shared = NomadWeatherService()`.
4. Method `func fetchTemperature(for coordinate: CLLocationCoordinate2D) async -> String?`:
   - Create `CLLocation` from coordinate.
   - Call `try await WeatherService.shared.weather(for: location)`.
   - Extract `weather.currentWeather.temperature` (a `Measurement<UnitTemperature>`).
   - Format using `MeasurementFormatter` with `.abbreviated` unit style so it returns e.g. "-8 C" — then manually format as "{value}{degree}{unit}" pattern like "-8°C". Use `temperature.value` rounded to Int and `temperature.unit.symbol`.
   - On any error (catch block), return `nil` — the UI-SPEC says hide the pill when unavailable.
5. No historical weather fallback — per RESEARCH.md recommendation, only current conditions; hide pill on failure.

Do NOT make this @Observable. It is a plain service class called from the ViewModel.
  </action>
  <verify>
    <automated>cd /Users/shrey/nomad-final/nomadv2 && xcodebuild -scheme Nomad -destination 'platform=iOS Simulator,name=iPhone 16 Pro' build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - WeatherService.swift compiles successfully
    - NomadWeatherService.shared.fetchTemperature returns Optional String
    - Returns nil on error (no crash, no throw)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → WeatherKit | External Apple service call with coordinate data |
| App → Photos | PHAsset access (not in this plan but entitlement-adjacent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.1-01 | Information Disclosure | WeatherService | accept | Coordinates sent to Apple WeatherKit are user's trip locations — Apple already has device location via CoreLocation; no additional PII exposure |
| T-3.1-02 | Denial of Service | WeatherService | mitigate | fetchTemperature wraps in try/catch returning nil; no crash on WeatherKit unavailability; pill hidden silently |
</threat_model>

<verification>
1. `xcodebuild build` succeeds with no errors
2. Nomad.entitlements contains both healthkit and weatherkit keys
3. CityCluster.swift and WeatherService.swift are present in Nomad/Data/
</verification>

<success_criteria>
- WeatherKit entitlement added to Nomad.entitlements (Wave 0 blocker resolved)
- CityCluster model with proximity clustering function compiles and is ready for consumption
- NomadWeatherService provides async temperature fetch with nil fallback
- All three files compile in the Nomad scheme
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-country-detail-view/03.1-01-SUMMARY.md`
</output>
