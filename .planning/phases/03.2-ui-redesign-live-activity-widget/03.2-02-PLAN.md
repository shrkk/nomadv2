---
phase: 03.2-ui-redesign-live-activity-widget
plan: 02
type: execute
wave: 2
depends_on: ["03.2-01"]
files_modified:
  - Nomad/Globe/GlobeView.swift
  - Nomad/Globe/GlobeViewModel.swift
  - Nomad/Sheets/TripDetailSheet.swift
  - Nomad/Sheets/CountryDetailSheet.swift
  - Nomad/Sheets/ProfileSheet.swift
  - Nomad/Components/CityPhotoCarousel.swift
  - Nomad/Components/TripRouteMapView.swift
  - Nomad/Components/JourneyPill.swift
  - Nomad/Components/RecordingPill.swift
  - Nomad/Components/StatsPillRow.swift
  - Nomad/Components/TripLogCard.swift
  - Nomad/Components/CityThumbnailCard.swift
  - Nomad/Components/TemperatureNotchPill.swift
  - Nomad/Components/PhotoGalleryStrip.swift
  - Nomad/Components/DragStrip.swift
  - Nomad/Components/RoutePreviewPath.swift
  - Nomad/App/ContentView.swift
  - Nomad/Onboarding/WelcomeScreen.swift
  - Nomad/Onboarding/HandleScreen.swift
  - Nomad/Onboarding/LocationPermissionScreen.swift
  - Nomad/Onboarding/PhotosPermissionScreen.swift
  - Nomad/Onboarding/DiscoveryScopeScreen.swift
  - Nomad/Onboarding/SignUpScreen.swift
  - Nomad/Onboarding/HomeCityScreen.swift
  - Nomad/Onboarding/OnboardingView.swift
  - Nomad/Onboarding/HealthPermissionScreen.swift
autonomous: true
requirements: []
must_haves:
  truths:
    - "Zero references to Color.Nomad.cream, Color.Nomad.warmCard, or Color.Nomad.amber remain in the entire Nomad/ directory"
    - "All bottom sheets use panelGlassSurface() or panelGradient() with .presentationBackground(Color.Nomad.panelBlack)"
    - "Globe country polygon highlight is white at 40% opacity instead of amber"
    - "Trip pinpoint dots on globe are white instead of amber"
    - "Recording pill dot is white with 0.8s pulse instead of red with 1.0s pulse"
    - "All text previously using Color.Nomad.globeBackground now uses Color.Nomad.textPrimary or textSecondary"
    - "Onboarding screens use panelBlack backgrounds with textPrimary/textSecondary text"
  artifacts:
    - path: "Nomad/Globe/GlobeView.swift"
      provides: "Redesigned globe chrome"
      contains: "textPrimary"
    - path: "Nomad/Sheets/TripDetailSheet.swift"
      provides: "Glass trip detail"
      contains: "panelBlack"
    - path: "Nomad/Components/RecordingPill.swift"
      provides: "White pulse recording pill"
      contains: "Color.white"
  key_links:
    - from: "all views"
      to: "Nomad/DesignSystem/AppColors.swift"
      via: "Color.Nomad.* tokens"
      pattern: "Color\\.Nomad\\.(panelBlack|textPrimary|textSecondary|accent|destructive)"
---

<objective>
Sweep all 25 files that reference old color tokens (cream/warmCard/amber) and update them to the new design system tokens from Plan 01. Also update globe chrome, polygon colors, pinpoint colors, pulse animation, and all sheet presentation backgrounds.

Purpose: Complete the visual redesign across every user-facing surface. After this plan, zero amber/cream references remain and the app renders in the new black/white glassmorphic aesthetic.

Output: All 25 files updated with new tokens. App compiles with zero old-token references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.2-ui-redesign-live-activity-widget/03.2-CONTEXT.md
@.planning/phases/03.2-ui-redesign-live-activity-widget/03.2-UI-SPEC.md
@.planning/phases/03.2-ui-redesign-live-activity-widget/03.2-01-SUMMARY.md

<interfaces>
<!-- New tokens available from Plan 01 (AppColors.swift): -->
```swift
Color.Nomad.globeBackground  // #0A0A14 — unchanged
Color.Nomad.panelBlack       // #0A0A0A — replaces cream
Color.Nomad.textPrimary      // white — replaces globeBackground for text
Color.Nomad.textSecondary    // white 60% — replaces globeBackground.opacity(0.6) for text
Color.Nomad.accent           // white — replaces amber for highlights
Color.Nomad.destructive      // #D94F3D — unchanged
Color.Nomad.star             // white — unchanged
```

<!-- New modifiers available from Plan 01 (PanelGradient.swift): -->
```swift
.panelGradient()             // backward-compatible alias -> panelGlassSurface()
.panelGlassSurface()         // 4-layer glass surface for sheets
.innerCardSurface()          // dark card surface for stats/trip cards
.floatingPillSurface()       // glass capsule for JourneyPill/RecordingPill
GlassButtonStyle()           // ButtonStyle with glass press state
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Globe chrome + Sheets + Main components token sweep</name>
  <files>
    Nomad/Globe/GlobeView.swift,
    Nomad/Sheets/TripDetailSheet.swift,
    Nomad/Sheets/CountryDetailSheet.swift,
    Nomad/Sheets/ProfileSheet.swift,
    Nomad/Components/JourneyPill.swift,
    Nomad/Components/RecordingPill.swift,
    Nomad/Components/StatsPillRow.swift,
    Nomad/Components/TripLogCard.swift,
    Nomad/Components/CityThumbnailCard.swift,
    Nomad/Components/CityPhotoCarousel.swift,
    Nomad/Components/TripRouteMapView.swift,
    Nomad/Components/TemperatureNotchPill.swift,
    Nomad/Components/PhotoGalleryStrip.swift,
    Nomad/Components/DragStrip.swift,
    Nomad/Components/RoutePreviewPath.swift,
    Nomad/App/ContentView.swift
  </files>
  <read_first>
    - Nomad/Globe/GlobeView.swift
    - Nomad/Sheets/TripDetailSheet.swift
    - Nomad/Sheets/CountryDetailSheet.swift
    - Nomad/Sheets/ProfileSheet.swift
    - Nomad/Components/JourneyPill.swift
    - Nomad/Components/RecordingPill.swift
    - Nomad/Components/StatsPillRow.swift
    - Nomad/Components/TripLogCard.swift
    - Nomad/Components/CityThumbnailCard.swift
    - Nomad/Components/CityPhotoCarousel.swift
    - Nomad/Components/TripRouteMapView.swift
    - Nomad/Components/TemperatureNotchPill.swift
    - Nomad/Components/PhotoGalleryStrip.swift
    - Nomad/Components/DragStrip.swift
    - Nomad/Components/RoutePreviewPath.swift
    - Nomad/App/ContentView.swift
    - .planning/phases/03.2-ui-redesign-live-activity-widget/03.2-UI-SPEC.md
  </read_first>
  <action>
Apply the following substitution rules across all files listed. For each file, read it first, then apply ALL applicable changes:

**Global substitution rules (apply everywhere):**

| Old Token | New Token | Notes |
|-----------|-----------|-------|
| `Color.Nomad.cream` | `Color.Nomad.panelBlack` | Background surfaces |
| `Color.Nomad.warmCard` | See context below | Inner cards, placeholders |
| `Color.Nomad.amber` | `Color.Nomad.accent` or `Color.Nomad.textPrimary` | See context below |
| `Color.Nomad.globeBackground` (as TEXT color) | `Color.Nomad.textPrimary` | All `.foregroundStyle(Color.Nomad.globeBackground)` |
| `Color.Nomad.globeBackground.opacity(0.6)` or `opacity(0.7)` (as TEXT) | `Color.Nomad.textSecondary` | Secondary text |
| `Color.Nomad.globeBackground.opacity(0.4)` (as TEXT) | `Color.Nomad.textSecondary` | Tertiary text |

**Context-specific amber -> accent/textPrimary mapping per UI-SPEC accent reserved list:**
- Key stat VALUES (steps, distance, duration numbers): `Color.Nomad.accent` (white)
- Button labels/icons: `Color.Nomad.textPrimary` (white)
- `plus.circle.fill` icon in JourneyPill: `Color.Nomad.accent`
- Selection indicators (CityThumbnailCard amber dot): `Color.Nomad.accent`
- Route polyline stroke in TripRouteMapView: `Color.Nomad.textPrimary`
- RoutePreviewPath stroke: `Color.Nomad.textPrimary`
- "Open Settings" link: `Color.Nomad.accent`
- Destructive buttons ("Stop Trip"): `Color.Nomad.destructive` (already correct in most places)

**Context-specific warmCard replacements:**
- Background of inner cards (StatsPillRow, TripLogCard, CityThumbnailCard placeholder): use `.innerCardSurface()` modifier from Plan 01
- Photo placeholder backgrounds in CityPhotoCarousel: `Color.Nomad.panelBlack`
- Map loading placeholder in TripRouteMapContainer: `Color.Nomad.panelBlack`

**File-specific instructions:**

**GlobeView.swift (lines 148-173 — polygon renderer + pinpoint annotations):**
- Line 148: `renderer.fillColor = UIColor.clear` -> `renderer.fillColor = UIColor.white.withAlphaComponent(0.40)`
- Line 149: `renderer.strokeColor = UIColor.clear` -> `renderer.strokeColor = UIColor.white.withAlphaComponent(0.60)`
- Line 150: `renderer.lineWidth = 0` -> `renderer.lineWidth = 1.0`
- Lines 166-167: Replace amber pinpoint dot color:
  ```swift
  let amber = UIColor(red: 0.910, green: 0.643, blue: 0.290, alpha: 1.0)
  amber.setFill()
  ```
  becomes:
  ```swift
  UIColor.white.setFill()
  ```

**JourneyPill.swift:**
- Apply `.floatingPillSurface()` modifier instead of `.background(Color.Nomad.warmCard).clipShape(Capsule()).shadow(...)` chain
- `person.circle` icon: `Color.Nomad.textPrimary`
- `plus.circle.fill` icon: `Color.Nomad.accent`
- "Journeys" label text: `Color.Nomad.textPrimary`
- Divider: `Color.white.opacity(0.20)` (replacing `Color.Nomad.globeBackground.opacity(0.2)`)

**RecordingPill.swift:**
- Dot color: `Color.red` -> `Color.white` (per D-04, UI-SPEC Pulsing Recording Dot)
- PulseAnimation modifier: duration `1.0` -> `0.8`, scaleEffect max `1.4` -> `1.5`, opacity min `0.6` -> `0.5`
- Elapsed text: `Color.Nomad.cream` -> `Color.Nomad.textPrimary`
- Stop Trip button text: keep `Color.Nomad.destructive` (correct)
- Stop Trip button: add destructive button variant per UI-SPEC:
  - Border: 1pt stroke `Color.Nomad.destructive.opacity(0.40)`
  - Fill: `Color.black.opacity(0.35)`
  - Shape: `Capsule`
- Pill background: replace existing background chain with `.floatingPillSurface()` modifier
- Amber stroke `Color.Nomad.amber.opacity(0.3)` -> white hairline `Color.white.opacity(0.20)` (handled by floatingPillSurface)

**TripDetailSheet.swift:**
- All `.foregroundStyle(Color.Nomad.globeBackground)` -> `.foregroundStyle(Color.Nomad.textPrimary)`
- All `.foregroundStyle(Color.Nomad.globeBackground.opacity(0.6))` -> `.foregroundStyle(Color.Nomad.textSecondary)`
- All `.foregroundStyle(Color.Nomad.globeBackground.opacity(0.5))` -> `.foregroundStyle(Color.Nomad.textSecondary)`
- "Maps" button: `.foregroundStyle(Color.Nomad.amber)` -> `.foregroundStyle(Color.Nomad.textPrimary)`, background `Color.Nomad.warmCard` -> use inline glass button style (RoundedRectangle cornerRadius 8, `Color.black.opacity(0.35)` fill, 1pt `Color.white.opacity(0.20)` border)
- Stats row background: `Color.Nomad.warmCard` -> apply `.innerCardSurface()` or inline `Color.black.opacity(0.35)` fill + 1pt `Color.white.opacity(0.15)` border
- Stat values: `Color.Nomad.amber` -> `Color.Nomad.accent`
- Top category icon: `Color.Nomad.amber` -> `Color.Nomad.accent`
- Add `.presentationBackground(Color.Nomad.panelBlack)` to sheet

**CountryDetailSheet.swift:**
- Back chevron: `Color.Nomad.amber` -> `Color.Nomad.textPrimary`
- Country name text: `Color.Nomad.globeBackground` -> `Color.Nomad.textPrimary`
- Empty state text: same substitution pattern
- Add `.presentationBackground(Color.Nomad.panelBlack)` to sheet

**ProfileSheet.swift:**
- `Color.Nomad.globeBackground` (text) -> `Color.Nomad.textPrimary`
- `Color.Nomad.amber` (plus icon) -> `Color.Nomad.accent`
- `.presentationBackground(Color.Nomad.cream)` -> `.presentationBackground(Color.Nomad.panelBlack)`
- TripPreviewCard: `Color.Nomad.warmCard` background -> `.innerCardSurface()` or `Color.black.opacity(0.35)` + border
- TripPreviewCard share button: `.background(Color.Nomad.amber)` -> `Color.Nomad.panelBlack` with white border
- DragStrip handle capsule: if referenced within ProfileSheet

**StatsPillRow.swift:**
- Values: `Color.Nomad.amber` -> `Color.Nomad.accent`
- Labels: `Color.Nomad.globeBackground.opacity(0.6)` -> `Color.Nomad.textSecondary`
- Background: `Color.Nomad.warmCard` -> apply inner card surface recipe: `Color.black.opacity(0.35)` fill + 1pt `Color.white.opacity(0.15)` RoundedRectangle stroke
- Dividers: `Color.Nomad.globeBackground.opacity(0.15)` -> `Color.white.opacity(0.15)`
- VStack spacing: `2` -> `4` per UI-SPEC checker revision

**TripLogCard.swift:**
- Text colors: globeBackground -> textPrimary/textSecondary
- Background: `Color.Nomad.warmCard` -> inner card surface
- Route strip mappin icon: `Color.Nomad.amber` -> `Color.Nomad.accent`

**CityThumbnailCard.swift:**
- Placeholder: `Color.Nomad.warmCard` -> `Color.Nomad.panelBlack`
- Selection stroke: `Color.Nomad.amber` -> `Color.white` (full opacity when selected per UI-SPEC)
- Unselected: 1pt `Color.white.opacity(0.15)` border
- Selection dot: `Color.Nomad.amber` -> `Color.white`
- City name label: `Color.Nomad.globeBackground` -> `Color.Nomad.textPrimary`
- Add scale animation: selected `scaleEffect(1.02)`, unselected `1.0`, with `.animation(.spring(response: 0.35, dampingFraction: 0.8), value: isSelected)` per UI-SPEC

**CityPhotoCarousel.swift:**
- Photo denied background: `Color.Nomad.warmCard` -> `Color.Nomad.panelBlack`
- "Open Settings" link: `Color.Nomad.amber` -> `Color.Nomad.accent`
- Empty placeholder background: `Color.Nomad.warmCard` -> `Color.Nomad.panelBlack`
- Empty placeholder text: `Color.Nomad.globeBackground.opacity(0.6)` -> `Color.Nomad.textSecondary`
- Location identity text: same substitution
- Preview background: `Color.Nomad.cream` -> `Color.Nomad.panelBlack`

**TripRouteMapView.swift:**
- Polyline stroke: `UIColor(Color.Nomad.amber)` -> `UIColor.white`
- Pin circle fill: `UIColor(Color.Nomad.amber)` -> `UIColor.white`
- Map container loading background: `Color.Nomad.warmCard` -> `Color.Nomad.panelBlack`

**TemperatureNotchPill.swift:**
- Text: `Color.Nomad.cream` -> `Color.Nomad.textPrimary`
- Border: `Color.Nomad.cream.opacity(0.2)` -> `Color.white.opacity(0.20)`
- Preview background: `Color.Nomad.cream` -> `Color.Nomad.panelBlack`

**PhotoGalleryStrip.swift:**
- "Open Settings" button: `Color.Nomad.amber` -> `Color.Nomad.accent`

**DragStrip.swift:**
- Background fill: `Color.Nomad.warmCard` -> glass surface: `Color.Nomad.panelBlack` with `.ultraThinMaterial` overlay
- Handle capsule: `Color.Nomad.amber.opacity(0.5)` -> `Color.white.opacity(0.30)` per UI-SPEC (36x4pt, 8pt top padding)
- Shadow adjustment if needed

**RoutePreviewPath.swift:**
- Stroke: `Color.Nomad.amber` -> `Color.Nomad.textPrimary`
- Preview background: `Color.Nomad.warmCard` -> `Color.Nomad.panelBlack`

**ContentView.swift:**
- All cream/warmCard/amber -> new tokens
- Handle: `Color.Nomad.warmCard` -> `Color.white.opacity(0.30)`
- Title: `Color.Nomad.globeBackground` -> `Color.Nomad.textPrimary`
- Subtitle: `Color.Nomad.amber` -> `Color.Nomad.accent`
- Body: `Color.Nomad.globeBackground.opacity(0.7)` -> `Color.Nomad.textSecondary`
- Caption: `Color.Nomad.globeBackground.opacity(0.4)` -> `Color.Nomad.textSecondary`
- Button: `Color.Nomad.cream` text -> `Color.Nomad.textPrimary`, `Color.Nomad.amber` bg -> use `.buttonStyle(GlassButtonStyle())`
  </action>
  <verify>
    <automated>grep -r "Color\.Nomad\.cream\|Color\.Nomad\.warmCard\|Color\.Nomad\.amber" Nomad/ --include="*.swift" | grep -v "\.planning" | wc -l | tr -d ' ' | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "Color.Nomad.cream\|Color.Nomad.warmCard\|Color.Nomad.amber" Nomad/ --include="*.swift"` returns 0 matches
    - `grep "UIColor.white.withAlphaComponent(0.40)" Nomad/Globe/GlobeView.swift` returns 1 match (polygon fill)
    - `grep "UIColor.white.withAlphaComponent(0.60)" Nomad/Globe/GlobeView.swift` returns 1 match (polygon stroke)
    - `grep "UIColor.white.setFill" Nomad/Globe/GlobeView.swift` returns 1 match (pinpoint dot)
    - `grep "Color.white" Nomad/Components/RecordingPill.swift` returns 1+ match (dot color)
    - `grep "duration: 0.8" Nomad/Components/RecordingPill.swift` returns 1 match (pulse)
    - `grep "1.5" Nomad/Components/RecordingPill.swift` returns 1 match (scale effect)
    - `grep "presentationBackground.*panelBlack" Nomad/Sheets/TripDetailSheet.swift` returns 1 match
    - `grep "presentationBackground.*panelBlack" Nomad/Sheets/CountryDetailSheet.swift` returns 1 match
    - `grep "presentationBackground.*panelBlack" Nomad/Sheets/ProfileSheet.swift` returns 1 match
    - `grep "Color.Nomad.textPrimary" Nomad/Sheets/TripDetailSheet.swift` returns 2+ matches
    - `grep "Color.Nomad.accent" Nomad/Components/StatsPillRow.swift` returns 1+ match
  </acceptance_criteria>
  <done>All 16 component/sheet/globe files updated — zero cream/warmCard/amber references. Globe polygons use white 40% fill. Pinpoints are white. Recording dot is white with 0.8s pulse. All sheets have panelBlack presentation background.</done>
</task>

<task type="auto">
  <name>Task 2: Onboarding screens token sweep</name>
  <files>
    Nomad/Onboarding/WelcomeScreen.swift,
    Nomad/Onboarding/HandleScreen.swift,
    Nomad/Onboarding/LocationPermissionScreen.swift,
    Nomad/Onboarding/PhotosPermissionScreen.swift,
    Nomad/Onboarding/DiscoveryScopeScreen.swift,
    Nomad/Onboarding/SignUpScreen.swift,
    Nomad/Onboarding/HomeCityScreen.swift,
    Nomad/Onboarding/OnboardingView.swift,
    Nomad/Onboarding/HealthPermissionScreen.swift
  </files>
  <read_first>
    - Nomad/Onboarding/WelcomeScreen.swift
    - Nomad/Onboarding/HandleScreen.swift
    - Nomad/Onboarding/LocationPermissionScreen.swift
    - Nomad/Onboarding/PhotosPermissionScreen.swift
    - Nomad/Onboarding/DiscoveryScopeScreen.swift
    - Nomad/Onboarding/SignUpScreen.swift
    - Nomad/Onboarding/HomeCityScreen.swift
    - Nomad/Onboarding/OnboardingView.swift
    - Nomad/Onboarding/HealthPermissionScreen.swift
    - .planning/phases/03.2-ui-redesign-live-activity-widget/03.2-UI-SPEC.md (Button Style Contract)
  </read_first>
  <action>
Apply the same global token substitution rules across all 9 onboarding files. The onboarding screens use the same tokens as main app screens.

**Substitution rules for onboarding:**

| Old Pattern | New Pattern |
|-------------|-------------|
| `.background(Color.Nomad.cream.ignoresSafeArea())` | `.background(Color.Nomad.panelBlack.ignoresSafeArea())` |
| `.background(Color.Nomad.cream)` | `.background(Color.Nomad.panelBlack)` |
| `.background(Color.Nomad.warmCard)` | Inner card: `Color.black.opacity(0.35)` fill + 1pt `Color.white.opacity(0.15)` border with `RoundedRectangle(cornerRadius: 12)` |
| `.foregroundColor(Color.Nomad.amber)` | `.foregroundColor(Color.Nomad.accent)` for interactive elements, `.foregroundColor(Color.Nomad.textPrimary)` for general icons |
| `.background(Color.Nomad.amber)` (CTA buttons) | `.background(Color.Nomad.accent)` with `.foregroundColor(Color.Nomad.panelBlack)` for text (white button with black text for primary CTAs) |
| `.background(Color.Nomad.amber.opacity(0.5))` (disabled CTA) | `.background(Color.Nomad.accent.opacity(0.3))` with `.foregroundColor(Color.Nomad.panelBlack)` |
| `Color.Nomad.amber.opacity(0.1)` (selected scope bg) | `Color.white.opacity(0.10)` |
| `Color.Nomad.amber : Color.Nomad.warmCard` (scope border ternary) | `Color.white : Color.white.opacity(0.15)` |
| `.tint(Color.Nomad.amber)` | `.tint(Color.Nomad.accent)` |

**OnboardingView.swift** (progress dots):
- Active dot: `Color.Nomad.amber` -> `Color.white`
- Inactive dot: `Color.Nomad.warmCard` -> `Color.white.opacity(0.30)`

**Key principle for onboarding CTAs:** Primary action buttons in onboarding should use inverted glass: white background (`Color.Nomad.accent`) with black text (`Color.Nomad.panelBlack`) to stand out on the dark background. This follows the accent rules — CTA buttons are interactive elements.

All existing text that used `Color.Nomad.globeBackground` for dark text on light background should switch to `Color.Nomad.textPrimary` (white) since backgrounds are now dark.
  </action>
  <verify>
    <automated>grep -r "Color\.Nomad\.cream\|Color\.Nomad\.warmCard\|Color\.Nomad\.amber" Nomad/Onboarding/ --include="*.swift" | wc -l | tr -d ' ' | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "Color.Nomad.cream\|Color.Nomad.warmCard\|Color.Nomad.amber" Nomad/Onboarding/ --include="*.swift"` returns 0 matches
    - `grep "panelBlack" Nomad/Onboarding/WelcomeScreen.swift` returns 1+ match
    - `grep "panelBlack" Nomad/Onboarding/SignUpScreen.swift` returns 1+ match
    - `grep "panelBlack" Nomad/Onboarding/HandleScreen.swift` returns 1+ match
    - All 9 onboarding files compile with the new token set (no references to removed tokens)
    - CTA button backgrounds use `Color.Nomad.accent` (white) with `Color.Nomad.panelBlack` text
  </acceptance_criteria>
  <done>All 9 onboarding files updated. Zero cream/warmCard/amber references in Nomad/Onboarding/. All screens render on dark backgrounds with white text.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | Pure visual token replacement — no user input changes, no network calls, no data access changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.2-02 | I (Information Disclosure) | GlobeView polygon renderer | accept | Country highlight color change is purely cosmetic — no data exposure change |
</threat_model>

<verification>
1. `grep -r "Color\.Nomad\.cream\|Color\.Nomad\.warmCard\|Color\.Nomad\.amber" Nomad/ --include="*.swift" | wc -l` returns 0
2. `grep -r "PlayfairDisplay" Nomad/ --include="*.swift" | wc -l` returns 0
3. All sheets have `.presentationBackground(Color.Nomad.panelBlack)`
4. Xcode build succeeds with zero unresolved token references
</verification>

<success_criteria>
- Zero references to cream, warmCard, or amber tokens in entire Nomad/ directory
- All 25 files using old tokens are updated
- Globe polygons render white at 40% opacity, pinpoints render white
- Recording pill pulses white at 0.8s cadence
- All sheets present on panelBlack background
- All onboarding screens render on dark background with white text
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-ui-redesign-live-activity-widget/03.2-02-SUMMARY.md`
</output>
